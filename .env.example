# Database Configuration
# Either set DATABASE_URL directly
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Or set individual PostgreSQL credentials (these will be used to construct the DATABASE_URL)
PGHOST=hostname
PGUSER=username
PGPASSWORD=password
PGDATABASE=database
PGPORT=5432

# Google Maps API Key
GOOGLE_MAPS_API_KEY=your_google_maps_api_key